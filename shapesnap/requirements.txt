# shapesnap python deps — run via uv (repo convention, see scripts/import-stop-areas.sh):
#   uv run --with-requirements shapesnap/requirements.txt python -m shapesnap.graph --mode rail
#   uv run --with-requirements shapesnap/requirements.txt python -m pytest shapesnap/tests -v
osmium>=4.0            # pyosmium: pbf/xml reading + location cache
psycopg[binary]>=3.1   # only needed for the --postgis QA dump
pytest>=8              # tests
shapely>=2.0           # matcher: STRtree candidates, substring, frechet, simplify
pyproj>=3.6            # matcher: local UTM projection
